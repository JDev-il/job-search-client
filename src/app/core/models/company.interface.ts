export interface Company {
  name: string,
  size: number, // how many employees?
  location: string, // should it be in coordinates based on google-maps api?
  lifespan: number, // in years
  applied: boolean
}
