export enum UsersMessages {
  exists = "User found! Redirecting you to your dashboard..",
  notExists = "User Not Found",
}

export enum AuthMessages {
  valid = "valid",
  invalid = "invalid",
  none = "no token"
}
